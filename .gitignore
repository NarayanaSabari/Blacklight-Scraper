node_modules/
results/
*.log
logs/

# Secrets — NEVER commit
config/credentials.json
config/linkedin-rsc-template.json
config/*.local.json
config/proxies.txt
config/cloakbrowser-keys.txt
config/platform-overrides.json
.env
.env.*
!.env.example

# Browser + crawler artifacts
crawlee_storage/
storage/
chrome-debug-profile/
# package-lock.json is COMMITTED: every script and doc uses npm, so the
# lockfile has to be in git for reproducible installs. A pnpm-lock.yaml
# used to live here instead, which made the documented `npm ci` impossible.
